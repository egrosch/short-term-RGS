For both datasets genomic marker data, yield data and a physical map are available. The genomic marker data can be found 
in files with the ending .mpo, yield data is in files with the ending .dta and files with .map contain the physical map 
positions of the markers.
